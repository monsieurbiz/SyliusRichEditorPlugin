{% form_theme form '@SyliusUi/Form/theme.html.twig' %}

<h1>{{ uiElement.title }}</h1>

<p>{{ uiElement.description }}</p>


{% if uiElement.type == 'image' and data.path is defined and data.path is not empty %}
    <p>{{ 'monsieurbiz_cmsplugin.ui_element.image.field.current_image' | trans }}</p>
    <p>
        <img src="{{ data.path }}" alt="{{ data.alt | default('') }}" title="{{ data.title | default('') }}" />
    </p>
{% endif %}

<p>
    {{ form(form) }}
</p>
