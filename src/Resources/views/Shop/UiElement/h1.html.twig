{#
    UI Element template
    type: h1
    element fields :
        content
        align
#}
{% set align = element.align is defined and element.align is not empty ? element.align : 'inherit' %}
{% if element is not empty %}
    <h1 class="ui h1" style="text-align: {{align}};">{{ element.content }}</h1>
{% endif %}
