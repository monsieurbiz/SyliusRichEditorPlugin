{#
    UI Element template
    type: image
    element fields :
        image
        alt
        title
#}
<p>
    <img src="{{ element.image }}" alt="{{ element.alt }}" title="{{ element.title }}" />
</p>
